# M2.1 — 페이지 CRUD + 사이드바 트리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사이드바 페이지 트리와 페이지 CRUD(생성/이름/아이콘/즐겨찾기/이동/삭제+Undo)를 RSC + Server Actions + `useOptimistic` 패턴으로 구현한다. document 타입 페이지만, 본문 에디터는 placeholder.

**Architecture:** 서버 컴포넌트가 한 번에 flat page 배열을 페치 → 클라이언트 컴포넌트가 nest해서 트리로 렌더. 모든 mutation은 Server Action(`lib/actions/pages.ts`), 낙관적 변경은 순수 reducer(`lib/pages/tree.ts`)와 `useOptimistic`. 펼침 상태는 localStorage. RLS·기존 마이그레이션·서비스롤 사용 정책은 손대지 않음.

**Tech Stack:** Next.js 15 (App Router) · React 19 (Server Components + `useOptimistic`) · Supabase (`@supabase/ssr`) · TypeScript strict · Zod (신규) · Sonner (신규 — 토스트) · emoji-mart (신규 — 이모지 피커) · vitest + Testing Library

**Spec:** `docs/superpowers/specs/2026-05-13-m2.1-page-crud-sidebar-tree-design.md`

---

## 사전 작업

### Task 1: 의존성 추가 + Sonner Toaster 설치

**Files:**
- Modify: `package.json` (자동, npm install로)
- Modify: `app/(app)/layout.tsx`

- [ ] **Step 1: 의존성 설치**

```bash
npm install zod sonner emoji-mart @emoji-mart/data @emoji-mart/react
```

Expected: 5개 패키지 추가, peer warning 없음.

- [ ] **Step 2: `app/(app)/layout.tsx`에 Sonner Toaster 마운트**

`app/(app)/layout.tsx` 끝의 `</div>` 직전에 `<Toaster />` 추가. import는 `import { Toaster } from "sonner";`. position은 기본(top-right)으로 두고 추후 디자인 통일.

```tsx
import { Toaster } from "sonner";
// ...
return (
  <div className="flex min-h-screen bg-background">
    {/* ... 기존 children ... */}
    <Toaster richColors closeButton />
  </div>
);
```

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: 에러 없음.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json app/\(app\)/layout.tsx
git commit -m "chore(M2.1): zod·sonner·emoji-mart 의존성 추가 + Toaster 마운트"
```

---

## 순수 도메인 코드 (TDD)

### Task 2: 페이지 타입 정의

**Files:**
- Create: `lib/pages/types.ts`

- [ ] **Step 1: 타입 파일 작성**

```ts
import type { Database } from "@/lib/database.types";

export type PageRow = Database["public"]["Tables"]["pages"]["Row"];

/**
 * 사이드바에서 사용하는 페이지 메타 (필요한 컬럼만 select).
 * title_encrypted, cover_url, is_private은 M2.1 범위 밖이라 제외.
 */
export interface PageNode {
  id: string;
  user_id: string;
  parent_page_id: string | null;
  type: "document" | "database";
  title: string | null;
  icon: string | null;
  is_favorite: boolean;
  position: number;
  created_at: string;
  updated_at: string;
}

export interface PageTreeNode extends PageNode {
  children: PageTreeNode[];
  depth: number;
}

export type OptimisticMutation =
  | { kind: "create"; node: PageNode }
  | { kind: "rename"; id: string; title: string }
  | { kind: "setIcon"; id: string; icon: string | null }
  | { kind: "toggleFavorite"; id: string; is_favorite: boolean }
  | {
      kind: "move";
      id: string;
      newParentId: string | null;
      newPosition: number;
    }
  | { kind: "softDelete"; deletedIds: string[] }
  | { kind: "restore"; restoredIds: string[] };

export type ActionResult<T> =
  | { ok: true; data: T }
  | { ok: false; error: string };
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: 에러 없음.

- [ ] **Step 3: Commit**

```bash
git add lib/pages/types.ts
git commit -m "feat(M2.1): PageNode/PageTreeNode/OptimisticMutation 타입 정의"
```

---

### Task 3: tree.ts — flat→nest + isDescendant + collectDescendantIds (TDD)

**Files:**
- Create: `lib/pages/tree.ts`
- Test: `lib/__tests__/tree.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// lib/__tests__/tree.test.ts
import { describe, it, expect } from "vitest";
import {
  buildTree,
  isDescendant,
  collectDescendantIds,
} from "@/lib/pages/tree";
import type { PageNode } from "@/lib/pages/types";

function makeNode(id: string, parent: string | null, position = 0): PageNode {
  return {
    id,
    user_id: "u1",
    parent_page_id: parent,
    type: "document",
    title: id,
    icon: null,
    is_favorite: false,
    position,
    created_at: "2026-05-13T00:00:00Z",
    updated_at: "2026-05-13T00:00:00Z",
  };
}

describe("buildTree", () => {
  it("빈 입력은 빈 배열", () => {
    expect(buildTree([])).toEqual([]);
  });

  it("position 오름차순 정렬", () => {
    const tree = buildTree([
      makeNode("b", null, 2),
      makeNode("a", null, 1),
    ]);
    expect(tree.map((n) => n.id)).toEqual(["a", "b"]);
  });

  it("부모-자식 nest + depth 부여", () => {
    const tree = buildTree([
      makeNode("p", null, 0),
      makeNode("c1", "p", 0),
      makeNode("c2", "p", 1),
      makeNode("gc", "c1", 0),
    ]);
    expect(tree).toHaveLength(1);
    expect(tree[0].id).toBe("p");
    expect(tree[0].depth).toBe(0);
    expect(tree[0].children.map((n) => n.id)).toEqual(["c1", "c2"]);
    expect(tree[0].children[0].depth).toBe(1);
    expect(tree[0].children[0].children[0].id).toBe("gc");
    expect(tree[0].children[0].children[0].depth).toBe(2);
  });

  it("고아 노드(부모 ID 가리키나 부모 없음)는 루트로 승격", () => {
    const tree = buildTree([makeNode("orphan", "missing-parent", 0)]);
    expect(tree).toHaveLength(1);
    expect(tree[0].id).toBe("orphan");
    expect(tree[0].depth).toBe(0);
  });
});

describe("isDescendant", () => {
  const nodes = [
    makeNode("a", null),
    makeNode("b", "a"),
    makeNode("c", "b"),
    makeNode("d", null),
  ];

  it("자기 자신은 후손으로 간주", () => {
    expect(isDescendant(nodes, "a", "a")).toBe(true);
  });

  it("직계 자식은 후손", () => {
    expect(isDescendant(nodes, "a", "b")).toBe(true);
  });

  it("손자는 후손", () => {
    expect(isDescendant(nodes, "a", "c")).toBe(true);
  });

  it("형제는 후손 아님", () => {
    expect(isDescendant(nodes, "a", "d")).toBe(false);
  });

  it("부모는 자식의 후손 아님(역방향)", () => {
    expect(isDescendant(nodes, "b", "a")).toBe(false);
  });
});

describe("collectDescendantIds", () => {
  const nodes = [
    makeNode("a", null),
    makeNode("b", "a"),
    makeNode("c", "a"),
    makeNode("d", "b"),
    makeNode("e", null),
  ];

  it("자기 + 모든 후손 ID 반환", () => {
    expect(new Set(collectDescendantIds(nodes, "a"))).toEqual(
      new Set(["a", "b", "c", "d"]),
    );
  });

  it("자식 없는 노드는 자기만", () => {
    expect(collectDescendantIds(nodes, "e")).toEqual(["e"]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- tree.test`
Expected: FAIL — `Cannot find module '@/lib/pages/tree'`.

- [ ] **Step 3: 구현**

```ts
// lib/pages/tree.ts
import type { PageNode, PageTreeNode } from "./types";

/**
 * flat 페이지 배열을 트리로 변환.
 * - position 오름차순 정렬 (같은 parent 내에서)
 * - 부모 ID가 가리키는 노드가 없으면(고아) 루트로 승격
 * - depth는 0부터 시작
 */
export function buildTree(nodes: readonly PageNode[]): PageTreeNode[] {
  const byId = new Map<string, PageTreeNode>();
  for (const n of nodes) {
    byId.set(n.id, { ...n, children: [], depth: 0 });
  }

  const roots: PageTreeNode[] = [];
  for (const n of nodes) {
    const tn = byId.get(n.id)!;
    const parent = n.parent_page_id ? byId.get(n.parent_page_id) : null;
    if (parent) {
      parent.children.push(tn);
    } else {
      roots.push(tn);
    }
  }

  const sortRec = (arr: PageTreeNode[], depth: number) => {
    arr.sort((a, b) => a.position - b.position);
    for (const node of arr) {
      node.depth = depth;
      sortRec(node.children, depth + 1);
    }
  };
  sortRec(roots, 0);

  return roots;
}

/**
 * targetId가 ancestorId의 후손인지 (자기 자신 포함).
 * movePage 사이클 방지에 사용.
 */
export function isDescendant(
  nodes: readonly PageNode[],
  ancestorId: string,
  targetId: string,
): boolean {
  if (ancestorId === targetId) return true;
  const childrenByParent = new Map<string, string[]>();
  for (const n of nodes) {
    if (n.parent_page_id) {
      const arr = childrenByParent.get(n.parent_page_id) ?? [];
      arr.push(n.id);
      childrenByParent.set(n.parent_page_id, arr);
    }
  }
  const stack = [ancestorId];
  while (stack.length) {
    const cur = stack.pop()!;
    if (cur === targetId) return true;
    for (const child of childrenByParent.get(cur) ?? []) stack.push(child);
  }
  return false;
}

/**
 * id 노드 자신 + 모든 후손 ID를 반환 (cascade soft delete용).
 */
export function collectDescendantIds(
  nodes: readonly PageNode[],
  id: string,
): string[] {
  const childrenByParent = new Map<string, string[]>();
  for (const n of nodes) {
    if (n.parent_page_id) {
      const arr = childrenByParent.get(n.parent_page_id) ?? [];
      arr.push(n.id);
      childrenByParent.set(n.parent_page_id, arr);
    }
  }
  const result: string[] = [];
  const stack = [id];
  while (stack.length) {
    const cur = stack.pop()!;
    result.push(cur);
    for (const child of childrenByParent.get(cur) ?? []) stack.push(child);
  }
  return result;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test -- tree.test`
Expected: 모든 케이스 PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/pages/tree.ts lib/__tests__/tree.test.ts
git commit -m "feat(M2.1): buildTree/isDescendant/collectDescendantIds 순수 함수 + 테스트"
```

---

### Task 4: 낙관적 reducer (TDD)

**Files:**
- Create: `lib/pages/reducer.ts`
- Test: `lib/__tests__/tree-reducer.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// lib/__tests__/tree-reducer.test.ts
import { describe, it, expect } from "vitest";
import { reduceTree } from "@/lib/pages/reducer";
import type { PageNode } from "@/lib/pages/types";

function makeNode(id: string, parent: string | null = null, extra: Partial<PageNode> = {}): PageNode {
  return {
    id,
    user_id: "u1",
    parent_page_id: parent,
    type: "document",
    title: id,
    icon: null,
    is_favorite: false,
    position: 0,
    created_at: "2026-05-13T00:00:00Z",
    updated_at: "2026-05-13T00:00:00Z",
    ...extra,
  };
}

describe("reduceTree", () => {
  it("rename 변경", () => {
    const out = reduceTree([makeNode("a")], { kind: "rename", id: "a", title: "Hello" });
    expect(out[0].title).toBe("Hello");
  });

  it("setIcon 변경 (null 가능)", () => {
    const out = reduceTree([makeNode("a", null, { icon: "🌸" })], { kind: "setIcon", id: "a", icon: null });
    expect(out[0].icon).toBe(null);
  });

  it("toggleFavorite", () => {
    const out = reduceTree([makeNode("a")], { kind: "toggleFavorite", id: "a", is_favorite: true });
    expect(out[0].is_favorite).toBe(true);
  });

  it("create — 새 노드 추가", () => {
    const newNode = makeNode("b", null, { position: 1 });
    const out = reduceTree([makeNode("a")], { kind: "create", node: newNode });
    expect(out).toHaveLength(2);
    expect(out.map((n) => n.id).sort()).toEqual(["a", "b"]);
  });

  it("move — parent_page_id + position 변경", () => {
    const initial = [makeNode("a"), makeNode("b")];
    const out = reduceTree(initial, { kind: "move", id: "b", newParentId: "a", newPosition: 5 });
    const b = out.find((n) => n.id === "b")!;
    expect(b.parent_page_id).toBe("a");
    expect(b.position).toBe(5);
  });

  it("softDelete cascade — 해당 ID 모두 제거", () => {
    const out = reduceTree(
      [makeNode("a"), makeNode("b", "a"), makeNode("c")],
      { kind: "softDelete", deletedIds: ["a", "b"] },
    );
    expect(out.map((n) => n.id)).toEqual(["c"]);
  });

  it("restore — 노드들 되돌리기는 다음 revalidate에 맡김(낙관적 reducer에서는 no-op)", () => {
    // restore는 서버 페치로 자연 수렴. reducer 단계에서는 추가 처리 없음.
    const out = reduceTree([makeNode("a")], { kind: "restore", restoredIds: ["b"] });
    expect(out.map((n) => n.id)).toEqual(["a"]);
  });

  it("입력을 mutate하지 않음 (immutability)", () => {
    const input = [makeNode("a", null, { title: "old" })];
    reduceTree(input, { kind: "rename", id: "a", title: "new" });
    expect(input[0].title).toBe("old");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- tree-reducer`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현**

```ts
// lib/pages/reducer.ts
import type { OptimisticMutation, PageNode } from "./types";

/**
 * 낙관적 mutation을 받아 새 페이지 flat 배열을 반환. 입력은 mutate하지 않음.
 * restore는 서버 페치로 자연 수렴되므로 reducer에서는 no-op.
 */
export function reduceTree(
  current: readonly PageNode[],
  mutation: OptimisticMutation,
): PageNode[] {
  switch (mutation.kind) {
    case "rename":
      return current.map((n) =>
        n.id === mutation.id ? { ...n, title: mutation.title } : n,
      );
    case "setIcon":
      return current.map((n) =>
        n.id === mutation.id ? { ...n, icon: mutation.icon } : n,
      );
    case "toggleFavorite":
      return current.map((n) =>
        n.id === mutation.id ? { ...n, is_favorite: mutation.is_favorite } : n,
      );
    case "create":
      return [...current, mutation.node];
    case "move":
      return current.map((n) =>
        n.id === mutation.id
          ? {
              ...n,
              parent_page_id: mutation.newParentId,
              position: mutation.newPosition,
            }
          : n,
      );
    case "softDelete": {
      const removeSet = new Set(mutation.deletedIds);
      return current.filter((n) => !removeSet.has(n.id));
    }
    case "restore":
      return [...current];
  }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test -- tree-reducer`
Expected: 모든 케이스 PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/pages/reducer.ts lib/__tests__/tree-reducer.test.ts
git commit -m "feat(M2.1): 낙관적 reduceTree 순수 함수 + 테스트"
```

---

### Task 5: position 계산 헬퍼 (TDD)

**Files:**
- Create: `lib/pages/position.ts`
- Test: `lib/__tests__/position.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// lib/__tests__/position.test.ts
import { describe, it, expect } from "vitest";
import { nextPosition } from "@/lib/pages/position";
import type { PageNode } from "@/lib/pages/types";

function n(id: string, parent: string | null, pos: number): PageNode {
  return {
    id, user_id: "u", parent_page_id: parent, type: "document",
    title: id, icon: null, is_favorite: false, position: pos,
    created_at: "", updated_at: "",
  };
}

describe("nextPosition", () => {
  it("빈 parent에서는 0", () => {
    expect(nextPosition([], null)).toBe(0);
  });

  it("같은 parent의 max + 1", () => {
    const nodes = [n("a", null, 0), n("b", null, 3), n("c", "x", 99)];
    expect(nextPosition(nodes, null)).toBe(4);
  });

  it("자식 parent 기준으로만 계산 (다른 parent 무시)", () => {
    const nodes = [n("a", "p1", 5), n("b", "p2", 99)];
    expect(nextPosition(nodes, "p1")).toBe(6);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- position.test`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현**

```ts
// lib/pages/position.ts
import type { PageNode } from "./types";

/** 같은 parent 내 max(position) + 1. 빈 경우 0. */
export function nextPosition(
  nodes: readonly Pick<PageNode, "parent_page_id" | "position">[],
  parentId: string | null,
): number {
  const siblings = nodes.filter((n) => n.parent_page_id === parentId);
  if (siblings.length === 0) return 0;
  return Math.max(...siblings.map((s) => s.position)) + 1;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test -- position.test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/pages/position.ts lib/__tests__/position.test.ts
git commit -m "feat(M2.1): nextPosition 헬퍼 + 테스트"
```

---

### Task 6: expanded-state — localStorage SSR-safe (TDD)

**Files:**
- Create: `lib/pages/expanded-state.ts`
- Test: `lib/__tests__/expanded-state.dom.test.ts` (jsdom 환경 — `.dom.test` 접미사)

- [ ] **Step 1: Write the failing test**

```ts
// lib/__tests__/expanded-state.dom.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import {
  loadExpandedIds,
  saveExpandedIds,
  toggleExpanded,
} from "@/lib/pages/expanded-state";

beforeEach(() => {
  localStorage.clear();
});

describe("expanded-state", () => {
  it("초기엔 빈 Set", () => {
    expect(loadExpandedIds().size).toBe(0);
  });

  it("save 후 load 가능", () => {
    saveExpandedIds(new Set(["a", "b"]));
    expect(loadExpandedIds()).toEqual(new Set(["a", "b"]));
  });

  it("toggle: 없으면 추가, 있으면 제거", () => {
    expect(toggleExpanded(new Set(), "a")).toEqual(new Set(["a"]));
    expect(toggleExpanded(new Set(["a"]), "a")).toEqual(new Set());
  });

  it("손상된 JSON 은 빈 Set 반환", () => {
    localStorage.setItem("younest:tree-expanded", "not-json");
    expect(loadExpandedIds().size).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- expanded-state`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현**

```ts
// lib/pages/expanded-state.ts
const KEY = "younest:tree-expanded";

/** SSR-safe localStorage 접근. 실패는 빈 Set으로 fallback. */
export function loadExpandedIds(): Set<string> {
  if (typeof window === "undefined") return new Set();
  try {
    const raw = window.localStorage.getItem(KEY);
    if (!raw) return new Set();
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return new Set();
    return new Set(parsed.filter((x): x is string => typeof x === "string"));
  } catch {
    return new Set();
  }
}

export function saveExpandedIds(ids: ReadonlySet<string>): void {
  if (typeof window === "undefined") return;
  try {
    window.localStorage.setItem(KEY, JSON.stringify([...ids]));
  } catch {
    // 사용 거부/할당량 초과 — 메모리 상태만 유효, silent fail
  }
}

export function toggleExpanded(
  current: ReadonlySet<string>,
  id: string,
): Set<string> {
  const next = new Set(current);
  if (next.has(id)) next.delete(id);
  else next.add(id);
  return next;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test -- expanded-state`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/pages/expanded-state.ts lib/__tests__/expanded-state.dom.test.ts
git commit -m "feat(M2.1): expanded-state localStorage 헬퍼 (SSR-safe) + 테스트"
```

---

## Supabase 쿼리 + soft delete RPC

### Task 7: Supabase 쿼리 헬퍼

**Files:**
- Create: `lib/supabase/queries/pages.ts`

- [ ] **Step 1: 쿼리 함수 작성**

```ts
// lib/supabase/queries/pages.ts
import { createSupabaseServerClient } from "@/lib/supabase/server";
import type { PageNode } from "@/lib/pages/types";

const PAGE_SELECT =
  "id, user_id, parent_page_id, type, title, icon, is_favorite, position, created_at, updated_at";

/**
 * 현재 사용자의 모든 살아있는(deleted_at IS NULL) 페이지를 flat 배열로 반환.
 * RLS가 자동으로 user_id = auth.uid() 필터링.
 */
export async function fetchUserPages(): Promise<PageNode[]> {
  const supabase = await createSupabaseServerClient();
  const { data, error } = await supabase
    .from("pages")
    .select(PAGE_SELECT)
    .is("deleted_at", null)
    .order("position", { ascending: true });

  if (error) throw error;
  return (data ?? []) as PageNode[];
}

/**
 * 단일 페이지 페치. 본인 페이지가 아니면 RLS로 null 반환.
 */
export async function fetchPage(pageId: string): Promise<PageNode | null> {
  const supabase = await createSupabaseServerClient();
  const { data, error } = await supabase
    .from("pages")
    .select(PAGE_SELECT)
    .eq("id", pageId)
    .is("deleted_at", null)
    .maybeSingle();

  if (error) throw error;
  return (data as PageNode | null) ?? null;
}
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: 에러 없음.

- [ ] **Step 3: Commit**

```bash
git add lib/supabase/queries/pages.ts
git commit -m "feat(M2.1): fetchUserPages/fetchPage 쿼리 헬퍼"
```

---

### Task 8: soft_delete_page_cascade RPC 마이그레이션

**Files:**
- Create: `supabase/migrations/20260513120000_pages_soft_delete_cascade.sql`

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 페이지 soft delete cascade RPC.
-- 자기 + 모든 후손을 한 번에 deleted_at = now()로 업데이트.
-- 반환된 ID 배열은 클라이언트 Undo에서 사용.
-- RLS가 user_id = auth.uid()만 통과시키므로 권한 보장.

create or replace function public.soft_delete_page_cascade(p_page_id uuid)
returns table (id uuid)
language plpgsql
security invoker
as $$
declare
  v_now timestamptz := now();
begin
  return query
  with recursive descendants as (
    select p.id
    from public.pages p
    where p.id = p_page_id and p.deleted_at is null
    union all
    select p.id
    from public.pages p
    join descendants d on p.parent_page_id = d.id
    where p.deleted_at is null
  ),
  updated as (
    update public.pages
    set deleted_at = v_now
    where pages.id in (select descendants.id from descendants)
      and pages.deleted_at is null
    returning pages.id
  )
  select updated.id from updated;
end;
$$;

comment on function public.soft_delete_page_cascade is
  'M2.1 — 페이지와 모든 후손을 한 번에 soft delete. 반환된 ID는 Undo에 사용.';
```

- [ ] **Step 2: 로컬 적용 (supabase가 연결돼 있는 경우)**

Run: `npx supabase db push` (또는 마이그레이션 확인 후 `mcp__supabase__apply_migration` 사용).
Expected: 새 함수가 적용됨.

> **참고**: 본 프로젝트는 클라우드 Supabase(`uausjolhamvofooyiijr`) 사용. MCP가 연결돼 있지 않으면 사용자에게 적용 안내.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260513120000_pages_soft_delete_cascade.sql
git commit -m "feat(M2.1): soft_delete_page_cascade RPC — 후손 cascade 삭제"
```

---

## Server Actions

### Task 9: lib/actions/pages.ts — createPage + renamePage

**Files:**
- Create: `lib/actions/pages.ts`
- Test: `lib/__tests__/actions-pages.test.ts`

- [ ] **Step 1: Write the failing test (Zod 검증만 — DB는 통합 단계에서)**

```ts
// lib/__tests__/actions-pages.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";

// Supabase 모킹 (DB 호출 차단, Zod·cycle 로직만 검증)
vi.mock("@/lib/supabase/server", () => ({
  createSupabaseServerClient: vi.fn(),
}));
vi.mock("@/lib/auth/session", () => ({
  getSessionUser: vi.fn(),
}));
vi.mock("next/cache", () => ({
  revalidatePath: vi.fn(),
}));

import { createPage, renamePage } from "@/lib/actions/pages";
import { getSessionUser } from "@/lib/auth/session";

beforeEach(() => {
  vi.clearAllMocks();
  vi.mocked(getSessionUser).mockResolvedValue({
    id: "u1",
    kakaoId: "k1",
    nickname: "user",
    status: "approved",
    isAdmin: false,
  });
});

describe("createPage Zod", () => {
  it("type이 document가 아니면 reject", async () => {
    const res = await createPage({ parentPageId: null, type: "database" as never });
    expect(res.ok).toBe(false);
  });

  it("title이 200자 초과 시 reject", async () => {
    const res = await createPage({
      parentPageId: null,
      type: "document",
      title: "x".repeat(201),
    });
    expect(res.ok).toBe(false);
  });
});

describe("renamePage Zod", () => {
  it("title이 200자 초과 시 reject", async () => {
    const res = await renamePage("00000000-0000-0000-0000-000000000000", "x".repeat(201));
    expect(res.ok).toBe(false);
  });

  it("id가 uuid 아니면 reject", async () => {
    const res = await renamePage("not-a-uuid", "hello");
    expect(res.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- actions-pages`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현 (createPage + renamePage만)**

```ts
// lib/actions/pages.ts
"use server";

import { revalidatePath } from "next/cache";
import { z } from "zod";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { getSessionUser } from "@/lib/auth/session";
import { nextPosition } from "@/lib/pages/position";
import type { ActionResult, PageNode } from "@/lib/pages/types";

const createSchema = z.object({
  parentPageId: z.string().uuid().nullable(),
  type: z.literal("document"),
  title: z.string().max(200).optional(),
});

const renameSchema = z.object({
  id: z.string().uuid(),
  title: z.string().max(200),
});

const PAGE_SELECT =
  "id, user_id, parent_page_id, type, title, icon, is_favorite, position, created_at, updated_at";

function revalidateAppLayout() {
  revalidatePath("/", "layout");
}

export async function createPage(input: {
  parentPageId: string | null;
  type: "document";
  title?: string;
}): Promise<ActionResult<PageNode>> {
  const parsed = createSchema.safeParse(input);
  if (!parsed.success) return { ok: false, error: "잘못된 입력입니다." };

  const me = await getSessionUser();
  if (!me) return { ok: false, error: "로그인이 필요합니다." };

  const supabase = await createSupabaseServerClient();

  // 같은 parent의 position 계산을 위해 형제 페이지 페치
  const { data: siblings } = await supabase
    .from("pages")
    .select("parent_page_id, position")
    .is("deleted_at", null)
    .eq("parent_page_id", parsed.data.parentPageId ?? null);

  const position = nextPosition(siblings ?? [], parsed.data.parentPageId);

  const { data, error } = await supabase
    .from("pages")
    .insert({
      user_id: me.id,
      parent_page_id: parsed.data.parentPageId,
      type: "document",
      title: parsed.data.title ?? null,
      position,
    })
    .select(PAGE_SELECT)
    .single();

  if (error) return { ok: false, error: error.message };

  revalidateAppLayout();
  return { ok: true, data: data as PageNode };
}

export async function renamePage(
  id: string,
  title: string,
): Promise<ActionResult<{ id: string; title: string }>> {
  const parsed = renameSchema.safeParse({ id, title });
  if (!parsed.success) {
    return { ok: false, error: "제목은 200자 이내로 입력해주세요." };
  }

  const supabase = await createSupabaseServerClient();
  const { error } = await supabase
    .from("pages")
    .update({ title: parsed.data.title })
    .eq("id", parsed.data.id);

  if (error) return { ok: false, error: error.message };

  revalidateAppLayout();
  revalidatePath(`/p/${parsed.data.id}`, "page");
  return { ok: true, data: { id: parsed.data.id, title: parsed.data.title } };
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test -- actions-pages`
Expected: PASS (모킹 덕분에 DB 미접근).

- [ ] **Step 5: Commit**

```bash
git add lib/actions/pages.ts lib/__tests__/actions-pages.test.ts
git commit -m "feat(M2.1): createPage·renamePage Server Action + Zod 검증 테스트"
```

---

### Task 10: setPageIcon + toggleFavorite

**Files:**
- Modify: `lib/actions/pages.ts`
- Modify: `lib/__tests__/actions-pages.test.ts`

- [ ] **Step 1: 테스트 추가**

```ts
// lib/__tests__/actions-pages.test.ts에 추가
import { setPageIcon, toggleFavorite } from "@/lib/actions/pages";

describe("setPageIcon Zod", () => {
  it("emoji 길이 16자 초과 시 reject", async () => {
    const res = await setPageIcon(
      "00000000-0000-0000-0000-000000000000",
      "x".repeat(17),
    );
    expect(res.ok).toBe(false);
  });
});

describe("toggleFavorite Zod", () => {
  it("uuid 아니면 reject", async () => {
    const res = await toggleFavorite("not-uuid");
    expect(res.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- actions-pages`
Expected: 추가 케이스 FAIL — import error.

- [ ] **Step 3: 구현 추가**

`lib/actions/pages.ts`에 추가:

```ts
const setIconSchema = z.object({
  id: z.string().uuid(),
  // 이모지 한 글자는 보통 ≤ 16 UTF-16 code units. 충분한 상한.
  icon: z.string().max(16).nullable(),
});

const idSchema = z.string().uuid();

export async function setPageIcon(
  id: string,
  icon: string | null,
): Promise<ActionResult<{ id: string; icon: string | null }>> {
  const parsed = setIconSchema.safeParse({ id, icon });
  if (!parsed.success) return { ok: false, error: "잘못된 아이콘입니다." };

  const supabase = await createSupabaseServerClient();
  const { error } = await supabase
    .from("pages")
    .update({ icon: parsed.data.icon })
    .eq("id", parsed.data.id);

  if (error) return { ok: false, error: error.message };
  revalidateAppLayout();
  revalidatePath(`/p/${parsed.data.id}`, "page");
  return { ok: true, data: { id: parsed.data.id, icon: parsed.data.icon } };
}

export async function toggleFavorite(
  id: string,
): Promise<ActionResult<{ id: string; is_favorite: boolean }>> {
  const parsed = idSchema.safeParse(id);
  if (!parsed.success) return { ok: false, error: "잘못된 페이지 ID입니다." };

  const supabase = await createSupabaseServerClient();

  // 현재 상태 읽어서 토글
  const { data: row, error: readErr } = await supabase
    .from("pages")
    .select("is_favorite")
    .eq("id", parsed.data)
    .maybeSingle();
  if (readErr || !row) {
    return { ok: false, error: "페이지를 찾을 수 없습니다." };
  }

  const next = !row.is_favorite;
  const { error } = await supabase
    .from("pages")
    .update({ is_favorite: next })
    .eq("id", parsed.data);

  if (error) return { ok: false, error: error.message };
  revalidateAppLayout();
  revalidatePath(`/p/${parsed.data}`, "page");
  return { ok: true, data: { id: parsed.data, is_favorite: next } };
}
```

- [ ] **Step 4: Run tests**

Run: `npm run test -- actions-pages`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/actions/pages.ts lib/__tests__/actions-pages.test.ts
git commit -m "feat(M2.1): setPageIcon·toggleFavorite Server Action"
```

---

### Task 11: movePage (cycle 방지 포함)

**Files:**
- Modify: `lib/actions/pages.ts`
- Modify: `lib/__tests__/actions-pages.test.ts`

- [ ] **Step 1: 테스트 추가**

```ts
// lib/__tests__/actions-pages.test.ts에 추가
import { movePage } from "@/lib/actions/pages";

describe("movePage Zod", () => {
  it("id가 uuid 아니면 reject", async () => {
    const res = await movePage("not-uuid", null);
    expect(res.ok).toBe(false);
  });
  it("newParentId가 잘못된 uuid면 reject", async () => {
    const res = await movePage(
      "00000000-0000-0000-0000-000000000000",
      "not-uuid",
    );
    expect(res.ok).toBe(false);
  });
});
```

- [ ] **Step 2: 구현 (isDescendant로 사이클 차단)**

`lib/actions/pages.ts`에 추가:

```ts
import { isDescendant } from "@/lib/pages/tree";
import type { PageNode } from "@/lib/pages/types";

const moveSchema = z.object({
  id: z.string().uuid(),
  newParentId: z.string().uuid().nullable(),
  newPosition: z.number().int().nonnegative().optional(),
});

export async function movePage(
  id: string,
  newParentId: string | null,
  newPosition?: number,
): Promise<
  ActionResult<{
    id: string;
    parent_page_id: string | null;
    position: number;
  }>
> {
  const parsed = moveSchema.safeParse({ id, newParentId, newPosition });
  if (!parsed.success) return { ok: false, error: "잘못된 이동 요청입니다." };

  const supabase = await createSupabaseServerClient();

  // 모든 페이지를 읽어 사이클 검증 (RLS로 본인 페이지만)
  const { data: all, error: readErr } = await supabase
    .from("pages")
    .select("id, parent_page_id, position")
    .is("deleted_at", null);
  if (readErr) return { ok: false, error: readErr.message };

  const allMin = (all ?? []) as Pick<
    PageNode,
    "id" | "parent_page_id" | "position"
  >[];

  if (parsed.data.newParentId) {
    if (
      isDescendant(
        allMin.map((m) => ({
          ...m,
          user_id: "",
          type: "document" as const,
          title: null,
          icon: null,
          is_favorite: false,
          created_at: "",
          updated_at: "",
        })),
        parsed.data.id,
        parsed.data.newParentId,
      )
    ) {
      return { ok: false, error: "자기 자신이나 후손으로는 이동할 수 없어요." };
    }
  }

  const position =
    parsed.data.newPosition ??
    nextPosition(allMin, parsed.data.newParentId);

  const { error } = await supabase
    .from("pages")
    .update({
      parent_page_id: parsed.data.newParentId,
      position,
    })
    .eq("id", parsed.data.id);

  if (error) return { ok: false, error: error.message };

  revalidateAppLayout();
  revalidatePath(`/p/${parsed.data.id}`, "page");
  return {
    ok: true,
    data: {
      id: parsed.data.id,
      parent_page_id: parsed.data.newParentId,
      position,
    },
  };
}
```

- [ ] **Step 3: Run tests**

Run: `npm run test -- actions-pages`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add lib/actions/pages.ts lib/__tests__/actions-pages.test.ts
git commit -m "feat(M2.1): movePage Server Action + 사이클 차단"
```

---

### Task 12: softDeletePage + restorePage

**Files:**
- Modify: `lib/actions/pages.ts`
- Modify: `lib/__tests__/actions-pages.test.ts`

- [ ] **Step 1: 테스트 추가**

```ts
// lib/__tests__/actions-pages.test.ts에 추가
import { softDeletePage, restorePage } from "@/lib/actions/pages";

describe("softDeletePage Zod", () => {
  it("uuid 아니면 reject", async () => {
    const res = await softDeletePage("not-uuid");
    expect(res.ok).toBe(false);
  });
});

describe("restorePage Zod", () => {
  it("빈 배열은 reject", async () => {
    const res = await restorePage([]);
    expect(res.ok).toBe(false);
  });
  it("uuid 아닌 ID 포함 시 reject", async () => {
    const res = await restorePage(["not-uuid"]);
    expect(res.ok).toBe(false);
  });
});
```

- [ ] **Step 2: 구현**

`lib/actions/pages.ts`에 추가:

```ts
const restoreSchema = z.array(z.string().uuid()).min(1);

export async function softDeletePage(
  id: string,
): Promise<ActionResult<{ deletedIds: string[] }>> {
  const parsed = idSchema.safeParse(id);
  if (!parsed.success) return { ok: false, error: "잘못된 페이지 ID입니다." };

  const supabase = await createSupabaseServerClient();
  const { data, error } = await supabase.rpc("soft_delete_page_cascade", {
    p_page_id: parsed.data,
  });

  if (error) return { ok: false, error: error.message };
  const deletedIds = ((data ?? []) as { id: string }[]).map((r) => r.id);

  revalidateAppLayout();
  return { ok: true, data: { deletedIds } };
}

export async function restorePage(
  ids: string[],
): Promise<ActionResult<{ restoredIds: string[] }>> {
  const parsed = restoreSchema.safeParse(ids);
  if (!parsed.success) return { ok: false, error: "복구할 페이지가 없습니다." };

  const supabase = await createSupabaseServerClient();
  const { error } = await supabase
    .from("pages")
    .update({ deleted_at: null })
    .in("id", parsed.data);

  if (error) return { ok: false, error: error.message };

  revalidateAppLayout();
  return { ok: true, data: { restoredIds: parsed.data } };
}
```

- [ ] **Step 3: Run tests**

Run: `npm run test -- actions-pages`
Expected: PASS.

- [ ] **Step 4: Supabase 타입 재생성**

새 RPC가 추가됐으므로 `Database` 타입 재생성. MCP 또는 CLI 사용:

```bash
npm run supabase:types
```

(MCP가 연결돼 있으면 `mcp__supabase__generate_typescript_types` 호출 후 출력을 `lib/database.types.ts`에 덮어쓰기.)

Expected: `lib/database.types.ts`에 `soft_delete_page_cascade` Functions 항목 추가.

- [ ] **Step 5: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add lib/actions/pages.ts lib/__tests__/actions-pages.test.ts lib/database.types.ts
git commit -m "feat(M2.1): softDeletePage·restorePage Server Action + 타입 재생성"
```

---

### Task 13: RLS 정적 검증 확장

**Files:**
- Modify: `lib/__tests__/rls-coverage.test.ts`

- [ ] **Step 1: 테스트 추가**

`lib/__tests__/rls-coverage.test.ts` 파일 끝에 새 describe 블록 추가:

```ts
describe("M2.1 RLS — pages Server Action service-role 미사용", () => {
  it("lib/actions/pages.ts가 createSupabaseAdminClient 호출 없음", () => {
    const content = readFileSync(join(REPO_ROOT, "lib/actions/pages.ts"), "utf-8");
    expect(content, "service-role client는 어드민 영역에서만 사용해야 합니다").not.toMatch(
      /createSupabaseAdminClient/,
    );
  });
});
```

- [ ] **Step 2: 테스트 실행**

Run: `npm run test -- rls-coverage`
Expected: 새 케이스 포함 모두 PASS (lib/actions/pages.ts는 service-role을 안 쓰니까).

- [ ] **Step 3: Commit**

```bash
git add lib/__tests__/rls-coverage.test.ts
git commit -m "test(M2.1): RLS 정적 검증에 pages Action service-role 미사용 추가"
```

---

## UI 컴포넌트 (외곽 → 안쪽 순)

### Task 14: 사이드바 트리 — flat→nest 렌더 (Sidebar 골격 + PageTree + PageTreeNode)

**Files:**
- Modify: `components/layout/Sidebar.tsx`
- Create: `components/layout/PageTree.tsx`
- Create: `components/layout/PageTreeNode.tsx`
- Create: `components/layout/EmptyPagesState.tsx`
- Modify: `app/(app)/layout.tsx`

- [ ] **Step 1: EmptyPagesState (단순 스타일)**

```tsx
// components/layout/EmptyPagesState.tsx
"use client";

import { createPage } from "@/lib/actions/pages";
import { useRouter } from "next/navigation";
import { useTransition } from "react";
import { toast } from "sonner";

export function EmptyPagesState() {
  const router = useRouter();
  const [pending, start] = useTransition();
  return (
    <div className="px-3 py-2">
      <p className="text-caption text-text-tertiary">아직 페이지가 없어요</p>
      <button
        type="button"
        disabled={pending}
        onClick={() =>
          start(async () => {
            const res = await createPage({ parentPageId: null, type: "document" });
            if (!res.ok) {
              toast.error(res.error);
              return;
            }
            router.push(`/p/${res.data.id}`);
          })
        }
        className="mt-2 text-body text-primary hover:underline disabled:opacity-50"
      >
        + 새 페이지
      </button>
    </div>
  );
}
```

- [ ] **Step 2: PageTreeNode (인덴트 + 펼침 토글 + 라벨)**

```tsx
// components/layout/PageTreeNode.tsx
"use client";

import Link from "next/link";
import type { PageTreeNode as Node } from "@/lib/pages/types";

interface Props {
  node: Node;
  expanded: ReadonlySet<string>;
  onToggleExpand: (id: string) => void;
}

export function PageTreeNode({ node, expanded, onToggleExpand }: Props) {
  const isOpen = expanded.has(node.id);
  const hasChildren = node.children.length > 0;
  return (
    <div>
      <div
        className="group flex items-center gap-1 rounded px-2 py-1 hover:bg-background"
        style={{ paddingLeft: `${8 + node.depth * 12}px` }}
      >
        <button
          type="button"
          aria-label={isOpen ? "접기" : "펼치기"}
          onClick={() => onToggleExpand(node.id)}
          className="h-4 w-4 text-text-tertiary"
        >
          {hasChildren ? (isOpen ? "▾" : "▸") : "·"}
        </button>
        <span className="w-4 text-center">{node.icon ?? "📄"}</span>
        <Link
          href={`/p/${node.id}`}
          className="flex-1 truncate text-body text-text-primary"
        >
          {node.title || "제목 없음"}
        </Link>
      </div>
      {isOpen &&
        node.children.map((child) => (
          <PageTreeNode
            key={child.id}
            node={child}
            expanded={expanded}
            onToggleExpand={onToggleExpand}
          />
        ))}
    </div>
  );
}
```

- [ ] **Step 3: PageTree (flat→nest + expanded state)**

```tsx
// components/layout/PageTree.tsx
"use client";

import { useEffect, useState, useMemo } from "react";
import { buildTree } from "@/lib/pages/tree";
import {
  loadExpandedIds,
  saveExpandedIds,
  toggleExpanded,
} from "@/lib/pages/expanded-state";
import type { PageNode } from "@/lib/pages/types";
import { PageTreeNode } from "./PageTreeNode";

interface Props {
  pages: PageNode[];
}

export function PageTree({ pages }: Props) {
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  // SSR 안전: 마운트 후에만 localStorage 읽기
  useEffect(() => {
    setExpanded(loadExpandedIds());
  }, []);

  const tree = useMemo(() => buildTree(pages), [pages]);

  const onToggle = (id: string) => {
    const next = toggleExpanded(expanded, id);
    setExpanded(next);
    saveExpandedIds(next);
  };

  return (
    <div className="space-y-px">
      {tree.map((node) => (
        <PageTreeNode
          key={node.id}
          node={node}
          expanded={expanded}
          onToggleExpand={onToggle}
        />
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Sidebar 개편 (트리 데이터 prop으로)**

```tsx
// components/layout/Sidebar.tsx
import Link from "next/link";
import type { PageNode } from "@/lib/pages/types";
import { PageTree } from "./PageTree";
import { EmptyPagesState } from "./EmptyPagesState";

interface SidebarProps {
  userName: string;
  pages: PageNode[];
}

export function Sidebar({ userName, pages }: SidebarProps) {
  const favorites = pages.filter((p) => p.is_favorite);
  const hasPages = pages.length > 0;
  return (
    <aside className="sticky top-0 hidden h-screen w-[240px] flex-shrink-0 flex-col border-r border-border bg-sidebar md:flex">
      <div className="px-4 py-6">
        <Link href="/dashboard" className="text-h1 font-bold text-primary hover:text-primary-hover">
          younest
        </Link>
      </div>
      <div className="px-4 pb-4">
        <button
          type="button"
          className="flex w-full items-center justify-between rounded-md border border-border bg-surface px-3 py-2 text-left text-body text-text-secondary hover:bg-background"
        >
          <span>검색...</span>
          <span className="text-caption text-text-tertiary">⌘K</span>
        </button>
      </div>

      <nav className="flex-1 overflow-y-auto px-2 py-2">
        {favorites.length > 0 && (
          <div className="px-2 py-3">
            <h3 className="px-2 text-caption uppercase tracking-wider text-text-tertiary">즐겨찾기</h3>
            <div className="mt-1">
              {favorites.slice(0, 8).map((p) => (
                <Link
                  key={p.id}
                  href={`/p/${p.id}`}
                  className="flex items-center gap-1 truncate rounded px-2 py-1 text-body text-text-primary hover:bg-background"
                >
                  <span className="w-4 text-center">{p.icon ?? "⭐"}</span>
                  <span className="truncate">{p.title || "제목 없음"}</span>
                </Link>
              ))}
            </div>
          </div>
        )}

        <div className="px-2 py-3">
          <h3 className="px-2 text-caption uppercase tracking-wider text-text-tertiary">내 페이지</h3>
          {hasPages ? <PageTree pages={pages} /> : <EmptyPagesState />}
        </div>
      </nav>

      <div className="border-t border-border px-4 py-3">
        <Link href="/trash" className="block py-1 text-body text-text-secondary hover:text-primary">
          🗑️ 휴지통
        </Link>
        <div className="mt-2 flex items-center justify-between">
          <p className="text-body font-medium text-text-primary">{userName}</p>
          <Link href="/settings" className="text-caption text-text-secondary hover:text-primary">
            설정
          </Link>
        </div>
      </div>
    </aside>
  );
}
```

- [ ] **Step 5: AppLayout에서 페이지 페치 후 prop 전달**

```tsx
// app/(app)/layout.tsx
import { redirect } from "next/navigation";
import { Toaster } from "sonner";
import { getSessionUser } from "@/lib/auth/session";
import { fetchUserPages } from "@/lib/supabase/queries/pages";
import { Sidebar } from "@/components/layout/Sidebar";
import { SidebarMobile } from "@/components/layout/SidebarMobile";

export default async function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const user = await getSessionUser();
  if (!user) redirect("/");
  if (user.status === "pending") redirect("/pending");
  if (user.status === "rejected" || user.status === "banned") redirect("/rejected");

  const pages = await fetchUserPages();

  return (
    <div className="flex min-h-screen bg-background">
      <Sidebar userName={user.nickname} pages={pages} />
      <div className="flex min-w-0 flex-1 flex-col">
        <div className="border-b border-border bg-surface p-2 md:hidden">
          <SidebarMobile userName={user.nickname} pages={pages} />
        </div>
        <main className="flex-1 overflow-y-auto">{children}</main>
      </div>
      <Toaster richColors closeButton />
    </div>
  );
}
```

- [ ] **Step 6: SidebarMobile prop 추가 (임시 — 단순 표시만)**

`components/layout/SidebarMobile.tsx`를 열어 props에 `pages: PageNode[]` 추가하고 내부도 PageTree 사용하도록 수정. (full mobile UX는 Task 21에서 완성.) 우선 컴파일이 깨지지 않도록 prop 받아서 안쪽 트리에 전달만.

```tsx
// components/layout/SidebarMobile.tsx (수정 — 인터페이스만 정합)
"use client";

import type { PageNode } from "@/lib/pages/types";
import { PageTree } from "./PageTree";

interface SidebarMobileProps {
  userName: string;
  pages: PageNode[];
}

export function SidebarMobile({ userName, pages }: SidebarMobileProps) {
  return (
    <div className="flex items-center justify-between">
      <span className="text-body font-medium">{userName}</span>
      {/* 풀 메뉴는 Task 21에서. 우선 prop만 받아 컴파일 통과 */}
      <details>
        <summary className="text-body text-primary">메뉴</summary>
        <div className="mt-2 max-h-[60vh] overflow-y-auto">
          <PageTree pages={pages} />
        </div>
      </details>
    </div>
  );
}
```

- [ ] **Step 7: typecheck + 수동 확인**

Run: `npm run typecheck && npm run lint`
Expected: 에러 없음.

- [ ] **Step 8: Commit**

```bash
git add components/layout/Sidebar.tsx components/layout/PageTree.tsx components/layout/PageTreeNode.tsx components/layout/EmptyPagesState.tsx components/layout/SidebarMobile.tsx app/\(app\)/layout.tsx
git commit -m "feat(M2.1): 사이드바 페이지 트리 기본 렌더(flat→nest, 펼침 상태 localStorage)"
```

---

### Task 15: 인라인 이름 편집 + IME 안전 처리 (PageTreeNode RTL 테스트 포함)

**Files:**
- Modify: `components/layout/PageTreeNode.tsx`
- Create: `components/layout/__tests__/PageTreeNode.dom.test.tsx`

- [ ] **Step 1: 실패하는 RTL 테스트**

```tsx
// components/layout/__tests__/PageTreeNode.dom.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { PageTreeNode } from "../PageTreeNode";
import type { PageTreeNode as Node } from "@/lib/pages/types";

function makeNode(over: Partial<Node> = {}): Node {
  return {
    id: "p1", user_id: "u", parent_page_id: null, type: "document",
    title: "Hello", icon: null, is_favorite: false, position: 0,
    created_at: "", updated_at: "", children: [], depth: 0,
    ...over,
  };
}

describe("PageTreeNode", () => {
  it("더블클릭 → 인라인 input 노출", () => {
    render(
      <PageTreeNode
        node={makeNode()}
        expanded={new Set()}
        onToggleExpand={vi.fn()}
        onRename={vi.fn()}
      />,
    );
    fireEvent.doubleClick(screen.getByText("Hello"));
    expect(screen.getByRole("textbox")).toBeInTheDocument();
  });

  it("Enter로 저장 → onRename 호출", () => {
    const onRename = vi.fn();
    render(
      <PageTreeNode
        node={makeNode()}
        expanded={new Set()}
        onToggleExpand={vi.fn()}
        onRename={onRename}
      />,
    );
    fireEvent.doubleClick(screen.getByText("Hello"));
    const input = screen.getByRole("textbox") as HTMLInputElement;
    fireEvent.change(input, { target: { value: "New" } });
    fireEvent.keyDown(input, { key: "Enter" });
    expect(onRename).toHaveBeenCalledWith("p1", "New");
  });

  it("Esc는 저장하지 않고 닫음", () => {
    const onRename = vi.fn();
    render(
      <PageTreeNode
        node={makeNode()}
        expanded={new Set()}
        onToggleExpand={vi.fn()}
        onRename={onRename}
      />,
    );
    fireEvent.doubleClick(screen.getByText("Hello"));
    const input = screen.getByRole("textbox") as HTMLInputElement;
    fireEvent.change(input, { target: { value: "New" } });
    fireEvent.keyDown(input, { key: "Escape" });
    expect(onRename).not.toHaveBeenCalled();
  });

  it("IME 변환 중 Enter는 저장하지 않음", () => {
    const onRename = vi.fn();
    render(
      <PageTreeNode
        node={makeNode()}
        expanded={new Set()}
        onToggleExpand={vi.fn()}
        onRename={onRename}
      />,
    );
    fireEvent.doubleClick(screen.getByText("Hello"));
    const input = screen.getByRole("textbox") as HTMLInputElement;
    fireEvent.compositionStart(input);
    fireEvent.change(input, { target: { value: "한글" } });
    fireEvent.keyDown(input, { key: "Enter" });
    expect(onRename).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- PageTreeNode`
Expected: FAIL — `onRename` prop, IME handling 없음.

- [ ] **Step 3: PageTreeNode 수정**

```tsx
// components/layout/PageTreeNode.tsx
"use client";

import Link from "next/link";
import { useRef, useState } from "react";
import type { PageTreeNode as Node } from "@/lib/pages/types";

interface Props {
  node: Node;
  expanded: ReadonlySet<string>;
  onToggleExpand: (id: string) => void;
  onRename: (id: string, title: string) => void;
}

export function PageTreeNode({
  node,
  expanded,
  onToggleExpand,
  onRename,
}: Props) {
  const isOpen = expanded.has(node.id);
  const hasChildren = node.children.length > 0;
  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState(node.title ?? "");
  const composingRef = useRef(false);

  const commit = () => {
    setEditing(false);
    const next = draft.trim();
    if (next !== (node.title ?? "")) onRename(node.id, next);
  };

  return (
    <div>
      <div
        className="group flex items-center gap-1 rounded px-2 py-1 hover:bg-background"
        style={{ paddingLeft: `${8 + node.depth * 12}px` }}
      >
        <button
          type="button"
          aria-label={isOpen ? "접기" : "펼치기"}
          onClick={() => onToggleExpand(node.id)}
          className="h-4 w-4 text-text-tertiary"
        >
          {hasChildren ? (isOpen ? "▾" : "▸") : "·"}
        </button>
        <span className="w-4 text-center">{node.icon ?? "📄"}</span>
        {editing ? (
          <input
            autoFocus
            value={draft}
            onChange={(e) => setDraft(e.target.value)}
            onCompositionStart={() => (composingRef.current = true)}
            onCompositionEnd={() => (composingRef.current = false)}
            onKeyDown={(e) => {
              if (e.key === "Enter" && !composingRef.current) commit();
              else if (e.key === "Escape") {
                setEditing(false);
                setDraft(node.title ?? "");
              }
            }}
            onBlur={commit}
            className="flex-1 rounded border border-border bg-surface px-1 py-0 text-body"
          />
        ) : (
          <Link
            href={`/p/${node.id}`}
            onDoubleClick={(e) => {
              e.preventDefault();
              setDraft(node.title ?? "");
              setEditing(true);
            }}
            className="flex-1 truncate text-body text-text-primary"
          >
            {node.title || "제목 없음"}
          </Link>
        )}
      </div>
      {isOpen &&
        node.children.map((child) => (
          <PageTreeNode
            key={child.id}
            node={child}
            expanded={expanded}
            onToggleExpand={onToggleExpand}
            onRename={onRename}
          />
        ))}
    </div>
  );
}
```

- [ ] **Step 4: PageTree에 onRename 와이어링 + 낙관적 업데이트**

```tsx
// components/layout/PageTree.tsx — useOptimistic + renamePage 호출 추가
"use client";

import { useEffect, useMemo, useOptimistic, useState, useTransition } from "react";
import { toast } from "sonner";
import { buildTree } from "@/lib/pages/tree";
import { reduceTree } from "@/lib/pages/reducer";
import { loadExpandedIds, saveExpandedIds, toggleExpanded } from "@/lib/pages/expanded-state";
import { renamePage } from "@/lib/actions/pages";
import type { PageNode, OptimisticMutation } from "@/lib/pages/types";
import { PageTreeNode } from "./PageTreeNode";

interface Props { pages: PageNode[] }

export function PageTree({ pages }: Props) {
  const [expanded, setExpanded] = useState<Set<string>>(new Set());
  const [optimistic, applyOptimistic] = useOptimistic(
    pages,
    (state: PageNode[], mutation: OptimisticMutation) => reduceTree(state, mutation),
  );
  const [, startTransition] = useTransition();

  useEffect(() => { setExpanded(loadExpandedIds()); }, []);

  const tree = useMemo(() => buildTree(optimistic), [optimistic]);

  const onToggle = (id: string) => {
    const next = toggleExpanded(expanded, id);
    setExpanded(next);
    saveExpandedIds(next);
  };

  const onRename = (id: string, title: string) => {
    startTransition(async () => {
      applyOptimistic({ kind: "rename", id, title });
      const res = await renamePage(id, title);
      if (!res.ok) toast.error(res.error);
    });
  };

  return (
    <div className="space-y-px">
      {tree.map((node) => (
        <PageTreeNode
          key={node.id}
          node={node}
          expanded={expanded}
          onToggleExpand={onToggle}
          onRename={onRename}
        />
      ))}
    </div>
  );
}
```

- [ ] **Step 5: Run tests**

Run: `npm run test -- PageTreeNode`
Expected: 4 케이스 모두 PASS.

- [ ] **Step 6: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add components/layout/PageTreeNode.tsx components/layout/PageTree.tsx components/layout/__tests__/PageTreeNode.dom.test.tsx
git commit -m "feat(M2.1): 사이드바 인라인 이름 편집 + IME 안전 처리 + 낙관적 업데이트"
```

---

### Task 16: hover "+" — 자식 페이지 생성 + 부모 자동 펼침

**Files:**
- Modify: `components/layout/PageTreeNode.tsx`
- Modify: `components/layout/PageTree.tsx`

- [ ] **Step 1: PageTreeNode에 hover "+" + 헤더 "+" 버튼**

`PageTreeNode` Props에 `onAddChild: (parentId: string) => void` 추가. 헤더 우측에 hover 시 노출되는 `+` 버튼:

```tsx
// (PageTreeNode 내 헤더 div의 마지막 자식에 추가)
<button
  type="button"
  aria-label="하위 페이지 추가"
  onClick={(e) => {
    e.preventDefault();
    onAddChild(node.id);
  }}
  className="invisible h-5 w-5 rounded text-text-tertiary group-hover:visible hover:bg-surface"
>
  +
</button>
```

interface에 `onAddChild` 추가, 자식 재귀 호출 시 그대로 전달.

- [ ] **Step 2: PageTree에 핸들러 + 루트 "+" 버튼**

`PageTree`에 다음 추가:

```tsx
import { createPage } from "@/lib/actions/pages";
import { useRouter } from "next/navigation";
// ...
const router = useRouter();

const onAddChild = (parentId: string | null) => {
  startTransition(async () => {
    const res = await createPage({ parentPageId: parentId, type: "document" });
    if (!res.ok) {
      toast.error(res.error);
      return;
    }
    // 자식 추가했으면 부모 자동 펼침
    if (parentId) {
      const next = new Set(expanded);
      next.add(parentId);
      setExpanded(next);
      saveExpandedIds(next);
    }
    router.push(`/p/${res.data.id}`);
  });
};
```

PageTree return 위쪽(트리 위)에 루트 추가 버튼 노출:

```tsx
<div className="mb-1 flex justify-end px-2">
  <button
    type="button"
    onClick={() => onAddChild(null)}
    className="rounded px-2 py-0.5 text-caption text-text-secondary hover:bg-background"
  >
    + 새 페이지
  </button>
</div>
```

`PageTreeNode` 호출 시 `onAddChild={onAddChild}` 전달.

- [ ] **Step 3: 수동 확인 (dev 서버 띄워서)**

```bash
npm run dev
```

브라우저에서 `/dashboard` 진입 → 사이드바 헤더 "+ 새 페이지" 클릭 → 페이지 생성 후 `/p/[id]`로 라우팅 확인. 페이지에 hover → "+" 버튼 노출, 클릭 시 자식 생성 + 부모 펼침 확인.

- [ ] **Step 4: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add components/layout/PageTreeNode.tsx components/layout/PageTree.tsx
git commit -m "feat(M2.1): 사이드바 hover ‘+’로 자식 페이지 생성 + 부모 자동 펼침"
```

---

### Task 17: 컨텍스트 메뉴 + 즐겨찾기 토글 + 아이콘 변경(이모지 피커)

**Files:**
- Create: `components/layout/PageContextMenu.tsx`
- Create: `components/layout/EmojiPickerPopover.tsx`
- Modify: `components/layout/PageTreeNode.tsx`
- Modify: `components/layout/PageTree.tsx`

- [ ] **Step 1: EmojiPickerPopover (emoji-mart wrapper)**

```tsx
// components/layout/EmojiPickerPopover.tsx
"use client";

import dynamic from "next/dynamic";
import data from "@emoji-mart/data";

// Picker는 클라이언트 전용 + 무겁기 때문에 dynamic import
const Picker = dynamic(() => import("@emoji-mart/react"), { ssr: false });

interface Props {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSelect: (emoji: string | null) => void;
}

export function EmojiPickerPopover({ open, onOpenChange, onSelect }: Props) {
  if (!open) return null;
  return (
    <div
      className="fixed inset-0 z-50 flex items-start justify-center bg-black/30 p-4"
      onClick={() => onOpenChange(false)}
    >
      <div onClick={(e) => e.stopPropagation()} className="mt-20">
        <Picker
          data={data}
          locale="ko"
          previewPosition="none"
          onEmojiSelect={(e: { native: string }) => {
            onSelect(e.native);
            onOpenChange(false);
          }}
        />
        <button
          type="button"
          onClick={() => {
            onSelect(null);
            onOpenChange(false);
          }}
          className="mt-2 w-full rounded bg-surface px-3 py-2 text-body text-text-secondary"
        >
          아이콘 제거
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: PageContextMenu (5개 항목 — Move/Delete 모달은 다음 태스크)**

```tsx
// components/layout/PageContextMenu.tsx
"use client";

import { useState } from "react";
import { EmojiPickerPopover } from "./EmojiPickerPopover";

interface Props {
  open: boolean;
  x: number;
  y: number;
  onClose: () => void;
  onAddChild: () => void;
  onToggleFavorite: () => void;
  onMove: () => void;
  onDelete: () => void;
  onSetIcon: (icon: string | null) => void;
  isFavorite: boolean;
}

export function PageContextMenu(props: Props) {
  const [emojiOpen, setEmojiOpen] = useState(false);
  if (!props.open) return null;
  return (
    <>
      <div className="fixed inset-0 z-40" onClick={props.onClose} />
      <div
        className="fixed z-50 w-48 rounded-md border border-border bg-surface py-1 shadow-lg"
        style={{ top: props.y, left: props.x }}
      >
        <Item onClick={() => { props.onAddChild(); props.onClose(); }}>+ 하위 페이지 추가</Item>
        <Item onClick={() => { props.onToggleFavorite(); props.onClose(); }}>
          {props.isFavorite ? "⭐ 즐겨찾기 해제" : "☆ 즐겨찾기 추가"}
        </Item>
        <Item onClick={() => setEmojiOpen(true)}>🎨 아이콘 변경</Item>
        <Item onClick={() => { props.onMove(); props.onClose(); }}>↗ 다른 페이지로 이동</Item>
        <Item onClick={() => { props.onDelete(); props.onClose(); }} danger>
          🗑️ 삭제
        </Item>
      </div>
      <EmojiPickerPopover
        open={emojiOpen}
        onOpenChange={setEmojiOpen}
        onSelect={(emoji) => { props.onSetIcon(emoji); props.onClose(); }}
      />
    </>
  );
}

function Item({
  children, onClick, danger,
}: { children: React.ReactNode; onClick: () => void; danger?: boolean }) {
  return (
    <button
      type="button"
      onClick={onClick}
      className={`w-full px-3 py-2 text-left text-body hover:bg-background ${
        danger ? "text-status-error" : "text-text-primary"
      }`}
    >
      {children}
    </button>
  );
}
```

- [ ] **Step 3: PageTreeNode에 우클릭 + ⋯ 버튼 + 메뉴 와이어링**

PageTreeNode Props에 다음 추가: `onToggleFavorite`, `onMove`, `onDelete`, `onSetIcon` (모두 `(id) => void` 또는 `(id, value) => void`).

헤더 div에 `onContextMenu` + ⋯ 버튼 추가, 메뉴 상태 관리:

```tsx
const [menuPos, setMenuPos] = useState<{ x: number; y: number } | null>(null);
// 헤더 div에:
onContextMenu={(e) => { e.preventDefault(); setMenuPos({ x: e.clientX, y: e.clientY }); }}
// hover 액션 옆에:
<button
  type="button"
  aria-label="메뉴"
  onClick={(e) => { e.preventDefault(); const r = e.currentTarget.getBoundingClientRect(); setMenuPos({ x: r.left, y: r.bottom }); }}
  className="invisible h-5 w-5 rounded text-text-tertiary group-hover:visible hover:bg-surface"
>⋯</button>

// JSX 끝:
<PageContextMenu
  open={menuPos !== null}
  x={menuPos?.x ?? 0}
  y={menuPos?.y ?? 0}
  onClose={() => setMenuPos(null)}
  onAddChild={() => onAddChild(node.id)}
  onToggleFavorite={() => onToggleFavorite(node.id)}
  onMove={() => onMove(node.id)}
  onDelete={() => onDelete(node.id)}
  onSetIcon={(icon) => onSetIcon(node.id, icon)}
  isFavorite={node.is_favorite}
/>
```

자식 재귀 호출 시 새 props 모두 전달.

- [ ] **Step 4: PageTree에 toggleFavorite + setPageIcon 와이어링 (Move/Delete는 다음 태스크에 모달 추가하면서 와이어링)**

```tsx
import { toggleFavorite, setPageIcon } from "@/lib/actions/pages";

const onToggleFav = (id: string) => {
  const current = optimistic.find((n) => n.id === id);
  if (!current) return;
  startTransition(async () => {
    applyOptimistic({ kind: "toggleFavorite", id, is_favorite: !current.is_favorite });
    const res = await toggleFavorite(id);
    if (!res.ok) toast.error(res.error);
  });
};

const onSetIcon = (id: string, icon: string | null) => {
  startTransition(async () => {
    applyOptimistic({ kind: "setIcon", id, icon });
    const res = await setPageIcon(id, icon);
    if (!res.ok) toast.error(res.error);
  });
};

// Move/Delete는 다음 태스크에서. 우선 빈 함수로 prop 채움:
const onMove = (_id: string) => { /* Task 18 */ };
const onDelete = (_id: string) => { /* Task 19 */ };

// PageTreeNode 호출에 추가:
onToggleFavorite={onToggleFav}
onSetIcon={onSetIcon}
onMove={onMove}
onDelete={onDelete}
```

- [ ] **Step 5: 수동 확인**

dev 서버로 우클릭 → 메뉴 → 즐겨찾기 토글(사이드바 즐겨찾기 섹션 등장/사라짐) + 아이콘 변경(이모지 선택/제거) 확인.

- [ ] **Step 6: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add components/layout/PageContextMenu.tsx components/layout/EmojiPickerPopover.tsx components/layout/PageTreeNode.tsx components/layout/PageTree.tsx
git commit -m "feat(M2.1): 페이지 컨텍스트 메뉴 + 즐겨찾기 토글 + 이모지 피커"
```

---

### Task 18: MovePageModal (검색 + 자기/후손 비활성화) + RTL 테스트

**Files:**
- Create: `components/layout/MovePageModal.tsx`
- Create: `components/layout/__tests__/MovePageModal.dom.test.tsx`
- Modify: `components/layout/PageTree.tsx`

- [ ] **Step 1: 실패하는 RTL 테스트**

```tsx
// components/layout/__tests__/MovePageModal.dom.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { MovePageModal } from "../MovePageModal";
import type { PageNode } from "@/lib/pages/types";

function n(id: string, parent: string | null, title: string): PageNode {
  return {
    id, user_id: "u", parent_page_id: parent, type: "document",
    title, icon: null, is_favorite: false, position: 0,
    created_at: "", updated_at: "",
  };
}

const pages: PageNode[] = [
  n("a", null, "Apple"),
  n("b", "a", "Banana (a의 자식)"),
  n("c", null, "Cherry"),
];

describe("MovePageModal", () => {
  it("자기 자신 옵션 비활성화", () => {
    render(
      <MovePageModal
        open
        movingId="a"
        pages={pages}
        onCancel={vi.fn()}
        onSubmit={vi.fn()}
      />,
    );
    const self = screen.getByRole("button", { name: /Apple/ });
    expect(self).toBeDisabled();
  });

  it("후손 옵션 비활성화", () => {
    render(
      <MovePageModal
        open
        movingId="a"
        pages={pages}
        onCancel={vi.fn()}
        onSubmit={vi.fn()}
      />,
    );
    expect(screen.getByRole("button", { name: /Banana/ })).toBeDisabled();
  });

  it("검색 필터 동작", () => {
    render(
      <MovePageModal
        open
        movingId="a"
        pages={pages}
        onCancel={vi.fn()}
        onSubmit={vi.fn()}
      />,
    );
    fireEvent.change(screen.getByPlaceholderText("페이지 검색..."), {
      target: { value: "Cherry" },
    });
    expect(screen.getByRole("button", { name: /Cherry/ })).toBeInTheDocument();
    expect(screen.queryByRole("button", { name: /Apple/ })).toBeNull();
  });

  it("‘최상위로 이동’ 클릭 → onSubmit(null)", () => {
    const onSubmit = vi.fn();
    render(
      <MovePageModal
        open
        movingId="a"
        pages={pages}
        onCancel={vi.fn()}
        onSubmit={onSubmit}
      />,
    );
    fireEvent.click(screen.getByRole("button", { name: "최상위로 이동" }));
    expect(onSubmit).toHaveBeenCalledWith(null);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- MovePageModal`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현**

```tsx
// components/layout/MovePageModal.tsx
"use client";

import { useMemo, useState } from "react";
import { isDescendant } from "@/lib/pages/tree";
import type { PageNode } from "@/lib/pages/types";

interface Props {
  open: boolean;
  movingId: string;
  pages: PageNode[];
  onCancel: () => void;
  onSubmit: (newParentId: string | null) => void;
}

export function MovePageModal({ open, movingId, pages, onCancel, onSubmit }: Props) {
  const [q, setQ] = useState("");

  const filtered = useMemo(() => {
    const ql = q.trim().toLowerCase();
    return pages.filter((p) =>
      ql ? (p.title ?? "").toLowerCase().includes(ql) : true,
    );
  }, [pages, q]);

  if (!open) return null;
  return (
    <div className="fixed inset-0 z-50 flex items-start justify-center bg-black/30 p-4" onClick={onCancel}>
      <div className="mt-20 w-[420px] rounded-md border border-border bg-surface p-3 shadow-lg" onClick={(e) => e.stopPropagation()}>
        <input
          autoFocus
          value={q}
          onChange={(e) => setQ(e.target.value)}
          placeholder="페이지 검색..."
          className="w-full rounded border border-border bg-background px-3 py-2 text-body"
        />
        <div className="mt-2 max-h-72 space-y-px overflow-y-auto">
          <button
            type="button"
            onClick={() => onSubmit(null)}
            className="w-full rounded px-3 py-2 text-left text-body text-primary hover:bg-background"
          >
            최상위로 이동
          </button>
          {filtered.map((p) => {
            const disabled = isDescendant(pages, movingId, p.id);
            return (
              <button
                key={p.id}
                type="button"
                disabled={disabled}
                onClick={() => onSubmit(p.id)}
                className="flex w-full items-center gap-2 rounded px-3 py-2 text-left text-body hover:bg-background disabled:opacity-40 disabled:hover:bg-transparent"
              >
                <span className="w-4 text-center">{p.icon ?? "📄"}</span>
                <span className="truncate">{p.title || "제목 없음"}</span>
              </button>
            );
          })}
        </div>
        <div className="mt-2 flex justify-end">
          <button type="button" onClick={onCancel} className="rounded px-3 py-1 text-body text-text-secondary">
            취소
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run tests**

Run: `npm run test -- MovePageModal`
Expected: 4 케이스 PASS.

- [ ] **Step 5: PageTree에 와이어링**

```tsx
import { MovePageModal } from "./MovePageModal";
import { movePage } from "@/lib/actions/pages";

const [movingId, setMovingId] = useState<string | null>(null);

const onMove = (id: string) => setMovingId(id);

const handleMoveSubmit = (newParentId: string | null) => {
  if (!movingId) return;
  const id = movingId;
  setMovingId(null);
  startTransition(async () => {
    // newPosition은 서버에서 nextPosition()으로 결정. 낙관적 변경은 임시 큰값.
    applyOptimistic({ kind: "move", id, newParentId, newPosition: Number.MAX_SAFE_INTEGER });
    const res = await movePage(id, newParentId);
    if (!res.ok) toast.error(res.error);
  });
};

// JSX:
{movingId && (
  <MovePageModal
    open
    movingId={movingId}
    pages={optimistic}
    onCancel={() => setMovingId(null)}
    onSubmit={handleMoveSubmit}
  />
)}
```

- [ ] **Step 6: 수동 확인**

dev 서버로 컨텍스트 메뉴 → "다른 페이지로 이동" → 모달 → 다른 페이지/최상위 선택 → 트리 갱신 확인. 자기 자신/후손 비활성화 확인.

- [ ] **Step 7: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add components/layout/MovePageModal.tsx components/layout/__tests__/MovePageModal.dom.test.tsx components/layout/PageTree.tsx
git commit -m "feat(M2.1): MovePageModal (검색 + 사이클 차단) + PageTree 와이어링"
```

---

### Task 19: DeleteConfirmModal + Undo 토스트 + RTL 테스트

**Files:**
- Create: `components/layout/DeleteConfirmModal.tsx`
- Create: `components/layout/__tests__/DeleteConfirmModal.dom.test.tsx`
- Modify: `components/layout/PageTree.tsx`

- [ ] **Step 1: 실패하는 RTL 테스트**

```tsx
// components/layout/__tests__/DeleteConfirmModal.dom.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { DeleteConfirmModal } from "../DeleteConfirmModal";

describe("DeleteConfirmModal", () => {
  it("자식 개수 노출", () => {
    render(
      <DeleteConfirmModal
        open
        childCount={3}
        onCancel={vi.fn()}
        onConfirm={vi.fn()}
      />,
    );
    expect(screen.getByText(/하위 페이지 3개/)).toBeInTheDocument();
  });

  it("확인 클릭 시 onConfirm 호출", () => {
    const onConfirm = vi.fn();
    render(
      <DeleteConfirmModal
        open
        childCount={2}
        onCancel={vi.fn()}
        onConfirm={onConfirm}
      />,
    );
    fireEvent.click(screen.getByRole("button", { name: "삭제" }));
    expect(onConfirm).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- DeleteConfirmModal`
Expected: FAIL — module not found.

- [ ] **Step 3: 구현**

```tsx
// components/layout/DeleteConfirmModal.tsx
"use client";

interface Props {
  open: boolean;
  childCount: number;
  onCancel: () => void;
  onConfirm: () => void;
}

export function DeleteConfirmModal({ open, childCount, onCancel, onConfirm }: Props) {
  if (!open) return null;
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4" onClick={onCancel}>
      <div className="w-[360px] rounded-md border border-border bg-surface p-4 shadow-lg" onClick={(e) => e.stopPropagation()}>
        <h2 className="text-h2 font-semibold">이 페이지를 삭제할까요?</h2>
        <p className="mt-2 text-body text-text-secondary">
          하위 페이지 {childCount}개도 같이 휴지통으로 이동돼요.
        </p>
        <div className="mt-4 flex justify-end gap-2">
          <button type="button" onClick={onCancel} className="rounded px-3 py-1 text-body text-text-secondary">
            취소
          </button>
          <button
            type="button"
            onClick={onConfirm}
            className="rounded bg-status-error px-3 py-1 text-body text-white"
          >
            삭제
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Run tests**

Run: `npm run test -- DeleteConfirmModal`
Expected: PASS.

- [ ] **Step 5: PageTree에 삭제 + Undo 와이어링**

```tsx
import { DeleteConfirmModal } from "./DeleteConfirmModal";
import { softDeletePage, restorePage } from "@/lib/actions/pages";
import { collectDescendantIds } from "@/lib/pages/tree";
import { useRouter, usePathname } from "next/navigation";

const pathname = usePathname();
const [deletingId, setDeletingId] = useState<string | null>(null);
const deletingChildren = deletingId
  ? collectDescendantIds(optimistic, deletingId).length - 1
  : 0;

const performDelete = (id: string) => {
  // 현재 라우트가 이 페이지면 대시보드로
  if (pathname === `/p/${id}`) router.push("/dashboard");

  startTransition(async () => {
    const localIds = collectDescendantIds(optimistic, id);
    applyOptimistic({ kind: "softDelete", deletedIds: localIds });
    const res = await softDeletePage(id);
    if (!res.ok) {
      toast.error(res.error);
      return;
    }
    const deletedIds = res.data.deletedIds;
    toast.success("휴지통으로 이동했어요", {
      duration: 5000,
      action: {
        label: "실행 취소",
        onClick: async () => {
          const undo = await restorePage(deletedIds);
          if (!undo.ok) toast.error(undo.error);
        },
      },
    });
  });
};

const onDelete = (id: string) => {
  const childCount = collectDescendantIds(optimistic, id).length - 1;
  if (childCount === 0) {
    performDelete(id);
  } else {
    setDeletingId(id);
  }
};

// JSX:
<DeleteConfirmModal
  open={deletingId !== null}
  childCount={deletingChildren}
  onCancel={() => setDeletingId(null)}
  onConfirm={() => {
    const id = deletingId!;
    setDeletingId(null);
    performDelete(id);
  }}
/>
```

- [ ] **Step 6: 수동 확인**

자식 없는 페이지 삭제 → 즉시 사라짐 + Undo 토스트 → 복구 동작 확인. 자식 있는 페이지 → 모달 → 삭제 → Undo로 부모+자식 모두 복구.

- [ ] **Step 7: typecheck + Commit**

Run: `npm run typecheck`

```bash
git add components/layout/DeleteConfirmModal.tsx components/layout/__tests__/DeleteConfirmModal.dom.test.tsx components/layout/PageTree.tsx
git commit -m "feat(M2.1): DeleteConfirmModal + 삭제 + Undo 토스트"
```

---

### Task 20: 페이지 헤더 — 아이콘 + 인라인 제목 + 즐겨찾기 별 + body placeholder

**Files:**
- Modify: `app/(app)/p/[pageId]/page.tsx`
- Create: `app/(app)/p/[pageId]/PageHeader.tsx`
- Create: `app/(app)/p/[pageId]/PageBodyPlaceholder.tsx`

- [ ] **Step 1: PageBodyPlaceholder**

```tsx
// app/(app)/p/[pageId]/PageBodyPlaceholder.tsx
export function PageBodyPlaceholder() {
  return (
    <div className="mt-6 rounded-md border border-dashed border-border bg-surface p-6 text-center text-body text-text-tertiary">
      본문 에디터는 M2.2에서 추가됩니다.
    </div>
  );
}
```

- [ ] **Step 2: PageHeader (client — 인라인 편집/즐겨찾기/이모지)**

```tsx
// app/(app)/p/[pageId]/PageHeader.tsx
"use client";

import { useRef, useState, useTransition } from "react";
import { toast } from "sonner";
import {
  renamePage,
  setPageIcon,
  toggleFavorite,
} from "@/lib/actions/pages";
import { EmojiPickerPopover } from "@/components/layout/EmojiPickerPopover";

interface Props {
  id: string;
  initialTitle: string | null;
  initialIcon: string | null;
  initialFavorite: boolean;
}

export function PageHeader({ id, initialTitle, initialIcon, initialFavorite }: Props) {
  const [title, setTitle] = useState(initialTitle ?? "");
  const [icon, setIcon] = useState(initialIcon);
  const [fav, setFav] = useState(initialFavorite);
  const [editing, setEditing] = useState(false);
  const [emojiOpen, setEmojiOpen] = useState(false);
  const [, start] = useTransition();
  const composingRef = useRef(false);

  const commitTitle = () => {
    setEditing(false);
    const next = title.trim();
    if (next === (initialTitle ?? "")) return;
    start(async () => {
      const res = await renamePage(id, next);
      if (!res.ok) toast.error(res.error);
    });
  };

  const onIconSelect = (next: string | null) => {
    setIcon(next);
    start(async () => {
      const res = await setPageIcon(id, next);
      if (!res.ok) toast.error(res.error);
    });
  };

  const onToggleFav = () => {
    setFav((v) => !v);
    start(async () => {
      const res = await toggleFavorite(id);
      if (!res.ok) toast.error(res.error);
    });
  };

  return (
    <div className="flex items-start gap-3">
      <button
        type="button"
        aria-label="아이콘 변경"
        onClick={() => setEmojiOpen(true)}
        className="text-h1"
      >
        {icon ?? "📄"}
      </button>
      <div className="flex-1">
        {editing ? (
          <input
            autoFocus
            value={title}
            onChange={(e) => setTitle(e.target.value)}
            onCompositionStart={() => (composingRef.current = true)}
            onCompositionEnd={() => (composingRef.current = false)}
            onKeyDown={(e) => {
              if (e.key === "Enter" && !composingRef.current) commitTitle();
              else if (e.key === "Escape") {
                setEditing(false);
                setTitle(initialTitle ?? "");
              }
            }}
            onBlur={commitTitle}
            className="w-full rounded border border-border bg-surface px-2 py-1 text-h1 font-semibold"
          />
        ) : (
          <h1
            className="cursor-text text-h1 font-semibold"
            onDoubleClick={() => setEditing(true)}
          >
            {title || "제목 없음"}
          </h1>
        )}
      </div>
      <button
        type="button"
        aria-label={fav ? "즐겨찾기 해제" : "즐겨찾기 추가"}
        onClick={onToggleFav}
        className="text-h1"
      >
        {fav ? "⭐" : "☆"}
      </button>
      <EmojiPickerPopover open={emojiOpen} onOpenChange={setEmojiOpen} onSelect={onIconSelect} />
    </div>
  );
}
```

- [ ] **Step 3: 페이지 라우트 수정**

```tsx
// app/(app)/p/[pageId]/page.tsx
import { notFound } from "next/navigation";
import { fetchPage } from "@/lib/supabase/queries/pages";
import { PageHeader } from "./PageHeader";
import { PageBodyPlaceholder } from "./PageBodyPlaceholder";

type PageProps = { params: Promise<{ pageId: string }> };

export default async function PageView({ params }: PageProps) {
  const { pageId } = await params;
  const page = await fetchPage(pageId);
  if (!page) notFound();

  return (
    <div className="mx-auto max-w-3xl p-6">
      <PageHeader
        id={page.id}
        initialTitle={page.title}
        initialIcon={page.icon}
        initialFavorite={page.is_favorite}
      />
      <PageBodyPlaceholder />
    </div>
  );
}
```

- [ ] **Step 4: 수동 확인 + typecheck**

Run: `npm run typecheck`
브라우저: 페이지 클릭 → 헤더에서 더블클릭 인라인 편집/이모지 변경/즐겨찾기 토글 동작 확인. 사이드바에도 즉시 반영(다음 새로고침이 아니라 revalidate로) 확인.

- [ ] **Step 5: Commit**

```bash
git add app/\(app\)/p/\[pageId\]/page.tsx app/\(app\)/p/\[pageId\]/PageHeader.tsx app/\(app\)/p/\[pageId\]/PageBodyPlaceholder.tsx
git commit -m "feat(M2.1): 페이지 헤더(아이콘·인라인 제목·즐겨찾기) + body placeholder"
```

---

### Task 21: 대시보드 빈 상태 + SidebarMobile 완성 (long-press 컨텍스트)

**Files:**
- Modify: `app/(app)/dashboard/page.tsx`
- Create: `components/layout/EmptyDashboard.tsx`
- Modify: `components/layout/SidebarMobile.tsx`

- [ ] **Step 1: EmptyDashboard**

```tsx
// components/layout/EmptyDashboard.tsx
"use client";

import { useRouter } from "next/navigation";
import { useTransition } from "react";
import { toast } from "sonner";
import { createPage } from "@/lib/actions/pages";

export function EmptyDashboard() {
  const router = useRouter();
  const [pending, start] = useTransition();
  return (
    <div className="mx-auto mt-16 max-w-md rounded-lg border border-dashed border-border bg-surface p-8 text-center">
      <h2 className="text-h2 font-semibold">첫 페이지를 만들어보세요</h2>
      <p className="mt-2 text-body text-text-secondary">
        일기, 메모, 기도제목 — 무엇이든 자유롭게 적을 수 있어요.
      </p>
      <button
        type="button"
        disabled={pending}
        onClick={() =>
          start(async () => {
            const res = await createPage({ parentPageId: null, type: "document" });
            if (!res.ok) {
              toast.error(res.error);
              return;
            }
            router.push(`/p/${res.data.id}`);
          })
        }
        className="mt-4 rounded-md bg-primary px-4 py-2 text-body font-medium text-primary-foreground disabled:opacity-50"
      >
        + 새 페이지
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 대시보드 수정**

```tsx
// app/(app)/dashboard/page.tsx
import { fetchUserPages } from "@/lib/supabase/queries/pages";
import { EmptyDashboard } from "@/components/layout/EmptyDashboard";

export default async function DashboardPage() {
  const pages = await fetchUserPages();
  if (pages.length === 0) return <EmptyDashboard />;

  return (
    <div className="mx-auto max-w-3xl p-6">
      <h1 className="text-h1 font-semibold">대시보드</h1>
      <p className="mt-2 text-body text-text-secondary">
        위젯은 M3에서 추가됩니다.
      </p>
    </div>
  );
}
```

- [ ] **Step 3: SidebarMobile 완성 (햄버거 오버레이 + 동일 트리)**

shadcn의 Sheet 사용 (`components/ui/sheet.tsx` 존재). 동일 PageTree + 즐겨찾기 섹션 표시:

```tsx
// components/layout/SidebarMobile.tsx
"use client";

import Link from "next/link";
import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet";
import type { PageNode } from "@/lib/pages/types";
import { PageTree } from "./PageTree";
import { EmptyPagesState } from "./EmptyPagesState";

interface SidebarMobileProps {
  userName: string;
  pages: PageNode[];
}

export function SidebarMobile({ userName, pages }: SidebarMobileProps) {
  const favorites = pages.filter((p) => p.is_favorite).slice(0, 8);
  return (
    <div className="flex items-center justify-between">
      <Sheet>
        <SheetTrigger className="rounded p-2 text-text-secondary">☰</SheetTrigger>
        <SheetContent side="left" className="w-[280px] overflow-y-auto p-3">
          <Link href="/dashboard" className="text-h2 font-bold text-primary">younest</Link>
          {favorites.length > 0 && (
            <div className="mt-4">
              <h3 className="text-caption uppercase text-text-tertiary">즐겨찾기</h3>
              {favorites.map((p) => (
                <Link key={p.id} href={`/p/${p.id}`} className="block truncate py-1 text-body">
                  {p.icon ?? "⭐"} {p.title || "제목 없음"}
                </Link>
              ))}
            </div>
          )}
          <div className="mt-4">
            <h3 className="text-caption uppercase text-text-tertiary">내 페이지</h3>
            {pages.length > 0 ? <PageTree pages={pages} /> : <EmptyPagesState />}
          </div>
          <div className="mt-6 border-t border-border pt-3 text-body">
            <Link href="/trash" className="block py-1 text-text-secondary">🗑️ 휴지통</Link>
            <Link href="/settings" className="block py-1 text-text-secondary">⚙️ 설정 ({userName})</Link>
          </div>
        </SheetContent>
      </Sheet>
      <span className="text-body font-medium">{userName}</span>
    </div>
  );
}
```

- [ ] **Step 4: PageTreeNode에 long-press 추가 (모바일 컨텍스트)**

`PageTreeNode` 내 헤더 div에:

```tsx
const longPressTimer = useRef<ReturnType<typeof setTimeout> | null>(null);
// 헤더 div에 onTouchStart/onTouchEnd:
onTouchStart={(e) => {
  const t = e.touches[0];
  longPressTimer.current = setTimeout(() => {
    setMenuPos({ x: t.clientX, y: t.clientY });
  }, 500);
}}
onTouchEnd={() => {
  if (longPressTimer.current) clearTimeout(longPressTimer.current);
}}
onTouchMove={() => {
  if (longPressTimer.current) clearTimeout(longPressTimer.current);
}}
```

- [ ] **Step 5: 수동 확인**

브라우저 dev 도구로 모바일 viewport 전환 → 햄버거 → 트리 동작 + long-press → 컨텍스트 메뉴 동작 확인.

- [ ] **Step 6: typecheck + lint + Commit**

Run: `npm run typecheck && npm run lint`

```bash
git add app/\(app\)/dashboard/page.tsx components/layout/EmptyDashboard.tsx components/layout/SidebarMobile.tsx components/layout/PageTreeNode.tsx
git commit -m "feat(M2.1): 대시보드 빈 상태 + 모바일 사이드바 햄버거 오버레이 + long-press 컨텍스트"
```

---

## 마무리

### Task 22: 전체 검증 (test, typecheck, lint, build) + 수동 체크리스트

**Files:** (없음 — 실행만)

- [ ] **Step 1: 모든 테스트**

Run: `npm run test:run`
Expected: 모든 케이스 PASS. M2.1 추가분 (tree, reducer, position, expanded-state, actions-pages, RLS 확장, 3개 RTL) 모두 GREEN.

- [ ] **Step 2: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: 에러 없음.

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 4: 수동 검증 체크리스트 (spec §5.수동)**

dev 서버(`npm run dev`)로 다음을 직접 확인:

- [ ] 빈 상태에서 "+ 새 페이지" → 페이지 생성 → `/p/[id]`로 라우팅 + 제목 포커스
- [ ] 사이드바 페이지 더블클릭 → 인라인 편집 → 변경 즉시 본문 헤더에도 반영
- [ ] 본문 헤더에서 이름변경 → 사이드바도 즉시 반영(낙관적)
- [ ] hover "+" → 자식 페이지 생성 → 부모 자동 펼침
- [ ] 우클릭 → 컨텍스트 메뉴 5개 항목(+ 아이콘 변경 6번째) 모두 노출
- [ ] 이모지 피커 → 선택 → 사이드바·본문 헤더 둘 다 이모지 표시 / 제거 가능
- [ ] 즐겨찾기 토글 → 즐겨찾기 섹션 등장 + 원래 위치에도 유지(중복 노출)
- [ ] 자식 없는 페이지 삭제 → 즉시 사라짐 + Undo 토스트 → 복구
- [ ] 자식 있는 페이지 삭제 → 모달 → 확인 → 부모+자식 모두 사라짐 → Undo로 통째 복구
- [ ] 이동 모달: 자기 자신/후손 선택 불가, "최상위로 이동" 동작
- [ ] 새로고침 후 펼침/접힘 상태 유지(localStorage)
- [ ] 모바일 viewport → 햄버거 → 동일 트리/모든 액션 동작
- [ ] 한글 IME로 제목 편집 시 변환 중 Enter가 저장 안 함
- [ ] 두 디바이스(브라우저 두 개)에서 동시 이름변경 → 마지막 쓰기가 이김

- [ ] **Step 5: M2.1 완료 보고 + 메모리 갱신**

체크리스트 통과 시:

```bash
git log --oneline | head -25
```

memory 파일에 M2.1 완료 기록 추가(별도 turn에서). PR/머지 흐름은 사용자 판단에 맡김.

---

## 자체 검토 (작성자 메모)

- Spec §0 범위 — 각 항목이 태스크에 매핑됨 (CRUD: T9-T12 / 트리: T14 / 인라인 편집: T15 / hover+자식: T16 / 메뉴+이모지+즐겨찾기: T17 / 이동: T18 / 삭제+Undo: T19 / 헤더: T20 / 빈상태+모바일: T21).
- Spec §1 의존성 — T1에서 zod/sonner/emoji-mart 모두 추가됨.
- Spec §2 파일 구조 — types(T2), tree(T3), reducer(T4), position(T5), expanded-state(T6), queries(T7), actions(T9-T12), 컴포넌트(T14-T21) 모두 정확한 경로로 명시됨.
- Spec §3 Server Action 시그니처 — 7개 액션 모두 구현됨 (createPage, renamePage, setPageIcon, toggleFavorite, movePage, softDeletePage, restorePage).
- Spec §3 Revalidation — `revalidatePath('/', 'layout')` 패턴 사용 (route group 정정 반영).
- Spec §4 엣지케이스 — IME (T15), cycle (T18 + T11), cascade soft delete (T8 + T12), 고아 노드 (T3), localStorage SSR-safe (T6), 현재 라우트 삭제 시 redirect (T19), Undo (T19), 한 줄 표시 + "제목 없음" fallback (T14·T15·T20) 처리.
- Spec §5 테스트 — 단위 4 (tree, reducer, position, expanded-state), Server Action 1 (actions-pages: Zod 검증), 컴포넌트 RTL 3 (PageTreeNode, MovePageModal, DeleteConfirmModal), RLS 정적 확장 1, 수동 14항목 모두 포함.
- 타입 일관성 — `PageNode` / `PageTreeNode` / `OptimisticMutation` / `ActionResult<T>` 일관 사용. reducer mutation kind와 액션 시그니처 1:1 매칭.
- Placeholder 없음 — 모든 step 실제 코드/명령어 포함.
- 마이그레이션은 추가만 (T8: 신규 파일), 기존 파일 수정 없음(Danger Zone 준수).

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-13-m2.1-page-crud-sidebar-tree.md`.**
